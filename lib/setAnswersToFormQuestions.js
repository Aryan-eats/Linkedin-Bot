import { buttonClasses, buttonClick } from "../constants/exports.js";
const setAnswersToFormQuestions = async (page, questionType) => {
  // Contact Info
  if (questionType === "Current CTC") return;

  // Expected CTC
  if (questionType === "Expected CTC") return;

  // Notice Period
  if (questionType === "Notice Period") return;

  // Total Years of Professional Experience
  if (questionType === "Total Professional Experience") return;

  // Additional Months of Experience
  if (questionType === "Additional Months of Experience") return;

  // Business Analysis/Program Management Experience in Pharma & Healthcare
  if (questionType === "Pharma & Healthcare Experience") return;

  // Pharmaceutical and Healthcare Related Experience
  if (questionType === "Pharmaceutical Experience") return;

  // Stakeholder Management Experience
  if (questionType === "Stakeholder Management Experience") return;

  // Python Programming Experience
  if (questionType === "Python Experience") return;

  // MBA Degree Completion
  if (questionType === "MBA Completion") return;

  // Consulting Experience
  if (questionType === "Consulting Experience") return;

  // Business Analyst Experience
  if (questionType === "Business Analyst Experience") return;

  // Counterparty Credit Risk Experience
  if (questionType === "Counterparty Credit Risk Experience") return;

  // Work From Office
  if (questionType === "Work From Office") return;

  // Contract Pay Comfortability
  if (questionType === "Contract Pay Comfortability") return;

  // Startup Experience
  if (questionType === "Startup Experience") return;

  // Cross-Functional Department Experience
  if (questionType === "Cross-Functional Experience") return;

  // Immediate Availability
  if (questionType === "Immediate Availability") return;
};

export default setAnswersToFormQuestions;
